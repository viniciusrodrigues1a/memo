import React from "react";
import { Text, View, FlatList, StyleSheet } from "react-native";

import { Board } from "../../../entities";

type BoardsFlatListProps = {
  data: Board[];
};

export default function BoardsFlatList({ data }: BoardsFlatListProps) {
  return (
  );
}

